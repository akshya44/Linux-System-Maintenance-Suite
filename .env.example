# ==== Suite Configuration (.env) ====

# Comma-separated list of absolute paths to back up.
BACKUP_SOURCES="/etc,/home"

# Where to store backup archives (must exist and be writable).
BACKUP_TARGET="/var/backups"

# How many days to keep backups. Older archives are pruned.
RETENTION_DAYS=7

# Log monitoring
# Comma-separated keywords to search for (case-insensitive).
LOG_KEYWORDS="error,failed,panic,oom,kernel: panic"

# Which log files or journals to check. If empty, defaults to: /var/log/syslog or journalctl
LOG_FILES="/var/log/syslog"

# Minutes to look back for alerts when using journalctl or timestamp filters
LOG_LOOKBACK_MINUTES=15

# Optional: e-mail to notify (requires local MTA/mailx). Leave empty to skip mail.
ALERT_RECIPIENT=""

# (Optional) Force a package manager: apt|dnf|pacman. Otherwise auto-detect.
PKG_MANAGER=""
